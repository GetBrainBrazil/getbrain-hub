import { useEffect, useState } from 'react';
import { toast } from 'sonner';
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Textarea } from '@/components/ui/textarea';
import { DEAL_STAGE_LABEL, DEAL_STAGES, DEAL_STAGE_PROBABILITY, PROJECT_TYPE_LABEL, PROJECT_TYPE_OPTIONS } from '@/constants/dealStages';
import { useCreateDeal } from '@/hooks/crm/useDeals';
import { useCompanies, useCrmActors, usePeopleByCompany } from '@/hooks/crm/useCrmReference';
import type { DealStage } from '@/types/crm';

export function NewDealDialog({ open, onOpenChange, defaultStage = 'presencial_agendada' }: { open: boolean; onOpenChange: (v: boolean) => void; defaultStage?: DealStage }) {
  const { data: companies = [] } = useCompanies();
  const { data: actors = [] } = useCrmActors();
  const createDeal = useCreateDeal();
  const [form, setForm] = useState({ title: '', company_id: '', contact_person_id: '', estimated_value: '', stage: defaultStage, project_type: '', expected_close_date: '', scope_summary: '', owner_actor_id: '' });
  const { data: people = [] } = usePeopleByCompany(form.company_id || null);
  useEffect(() => { setForm((f) => ({ ...f, stage: defaultStage })); }, [defaultStage]);
  useEffect(() => { if (!form.owner_actor_id && actors[0]) setForm((f) => ({ ...f, owner_actor_id: actors[0].id })); }, [actors, form.owner_actor_id]);
  const submit = () => createDeal.mutate({ title: form.title, company_id: form.company_id, contact_person_id: form.contact_person_id || null, stage: form.stage as DealStage, estimated_value: form.estimated_value ? Number(form.estimated_value) : null, probability_pct: DEAL_STAGE_PROBABILITY[form.stage as DealStage], expected_close_date: form.expected_close_date || null, project_type: form.project_type || null, scope_summary: form.scope_summary || null, owner_actor_id: form.owner_actor_id || null }, { onSuccess: (deal) => { toast.success(`Deal ${deal.code} criado`); onOpenChange(false); } });
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-xl"><DialogHeader><DialogTitle>Novo Deal</DialogTitle></DialogHeader><div className="grid gap-4 py-2"><div className="space-y-1.5"><Label>Título</Label><Input value={form.title} onChange={(e) => setForm((f) => ({ ...f, title: e.target.value }))} /></div><div className="grid gap-3 md:grid-cols-2"><div className="space-y-1.5"><Label>Empresa</Label><Select value={form.company_id} onValueChange={(v) => setForm((f) => ({ ...f, company_id: v, contact_person_id: '' }))}><SelectTrigger><SelectValue placeholder="Selecionar" /></SelectTrigger><SelectContent>{companies.map((c) => <SelectItem key={c.id} value={c.id}>{c.trade_name || c.legal_name}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1.5"><Label>Contato</Label><Select value={form.contact_person_id || 'none'} onValueChange={(v) => setForm((f) => ({ ...f, contact_person_id: v === 'none' ? '' : v }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem contato</SelectItem>{people.map((p) => <SelectItem key={p.id} value={p.id}>{p.full_name}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-3 md:grid-cols-3"><div className="space-y-1.5"><Label>Valor</Label><Input type="number" value={form.estimated_value} onChange={(e) => setForm((f) => ({ ...f, estimated_value: e.target.value }))} /></div><div className="space-y-1.5"><Label>Estágio</Label><Select value={form.stage} onValueChange={(v) => setForm((f) => ({ ...f, stage: v as DealStage }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{DEAL_STAGES.map((s) => <SelectItem key={s} value={s}>{DEAL_STAGE_LABEL[s]}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1.5"><Label>Fechamento</Label><Input type="date" value={form.expected_close_date} onChange={(e) => setForm((f) => ({ ...f, expected_close_date: e.target.value }))} /></div></div><div className="grid gap-3 md:grid-cols-2"><div className="space-y-1.5"><Label>Tipo</Label><Select value={form.project_type || 'none'} onValueChange={(v) => setForm((f) => ({ ...f, project_type: v === 'none' ? '' : v }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem tipo</SelectItem>{PROJECT_TYPE_OPTIONS.map((p) => <SelectItem key={p} value={p}>{PROJECT_TYPE_LABEL[p]}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1.5"><Label>Owner</Label><Select value={form.owner_actor_id} onValueChange={(v) => setForm((f) => ({ ...f, owner_actor_id: v }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{actors.map((a) => <SelectItem key={a.id} value={a.id}>{a.display_name}</SelectItem>)}</SelectContent></Select></div></div><div className="space-y-1.5"><Label>Resumo do escopo</Label><Textarea value={form.scope_summary} onChange={(e) => setForm((f) => ({ ...f, scope_summary: e.target.value }))} /></div></div><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Cancelar</Button><Button onClick={submit} disabled={!form.title || !form.company_id || (form.stage === 'orcamento_enviado' && !form.estimated_value)}>Criar deal</Button></DialogFooter></DialogContent></Dialog>;
}
