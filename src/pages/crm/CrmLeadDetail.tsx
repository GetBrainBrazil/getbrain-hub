import { useEffect, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { addMonths, format } from 'date-fns';
import { toast } from 'sonner';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Skeleton } from '@/components/ui/skeleton';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Textarea } from '@/components/ui/textarea';
import { ActivityPanel } from '@/components/crm/ActivityPanel';
import { DetailBreadcrumb, DetailShell, InfoBadge } from '@/components/crm/CrmDetailShared';
import { MarkdownSplitEditor } from '@/components/dev/MarkdownComposer';
import { DEAL_STAGE_LABEL, DEAL_STAGES, PROJECT_TYPE_LABEL, PROJECT_TYPE_OPTIONS } from '@/constants/dealStages';
import { useActivitiesForEntity, useConvertLeadToDealFull, useEntityAudit, useLeadByCode, useUpdateLeadField } from '@/hooks/crm/useCrmDetails';
import { useDeleteLead } from '@/hooks/crm/useLeads';
import { DangerZone } from '@/components/crm/DangerZone';
import { useQueryClient } from '@tanstack/react-query';
import { invalidateCrmCaches } from '@/lib/cacheInvalidation';
import { useCrmActors, usePeopleByCompany } from '@/hooks/crm/useCrmReference';
import { formatCurrency } from '@/lib/formatters';
import type { DealStage, Lead, LeadStatus } from '@/types/crm';

const LEAD_STATUS: LeadStatus[] = ['novo', 'triagem_agendada', 'triagem_feita', 'descartado', 'convertido'];
const LEAD_LABEL: Record<LeadStatus, string> = { novo: 'Novo', triagem_agendada: 'Triagem Agendada', triagem_feita: 'Triagem Feita', descartado: 'Descartado', convertido: 'Convertido' };

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="space-y-1.5"><Label className="text-[11px] font-semibold uppercase tracking-wide text-muted-foreground">{label}</Label>{children}</div>; }
function MiniTimeline({ rows }: { rows: { id: string; created_at: string; action: string }[] }) { return <div className="space-y-3">{rows.length ? rows.map((r) => <article key={r.id} className="rounded-lg border border-border bg-card/40 p-4 text-sm"><p>{r.action}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(r.created_at).toLocaleString('pt-BR')}</p></article>) : <div className="rounded-lg border border-dashed border-border p-8 text-center text-sm text-muted-foreground">Nenhum evento registrado.</div>}</div>; }

function ConvertDialog({ lead, open, onOpenChange }: { lead: Lead | null; open: boolean; onOpenChange: (v: boolean) => void }) {
  const navigate = useNavigate();
  const convert = useConvertLeadToDealFull();
  const { data: actors = [] } = useCrmActors();
  const [form, setForm] = useState({ title: '', stage: 'presencial_agendada' as DealStage, estimated_value: '', project_type: 'sistema_personalizado', expected_close_date: format(addMonths(new Date(), 1), 'yyyy-MM-dd'), owner_actor_id: 'none' });
  useEffect(() => { if (lead && open) setForm({ title: lead.title, stage: 'presencial_agendada', estimated_value: String(lead.estimated_value ?? ''), project_type: 'sistema_personalizado', expected_close_date: format(addMonths(new Date(), 1), 'yyyy-MM-dd'), owner_actor_id: lead.owner_actor_id ?? 'none' }); }, [lead, open]);
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-2xl"><DialogHeader><DialogTitle>Converter {lead?.code} em Deal</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><div className="space-y-2 sm:col-span-2"><Label>Título</Label><Input value={form.title} onChange={(e) => setForm((f) => ({ ...f, title: e.target.value }))} /></div><div className="space-y-2"><Label>Estágio inicial</Label><Select value={form.stage} onValueChange={(v) => setForm((f) => ({ ...f, stage: v as DealStage }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{DEAL_STAGES.filter((s) => !s.startsWith('fechado')).map((s) => <SelectItem key={s} value={s}>{DEAL_STAGE_LABEL[s]}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label>Valor estimado</Label><Input type="number" value={form.estimated_value} onChange={(e) => setForm((f) => ({ ...f, estimated_value: e.target.value }))} /></div><div className="space-y-2"><Label>Tipo</Label><Select value={form.project_type} onValueChange={(v) => setForm((f) => ({ ...f, project_type: v }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{PROJECT_TYPE_OPTIONS.map((p) => <SelectItem key={p} value={p}>{PROJECT_TYPE_LABEL[p]}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label>Fechamento previsto</Label><Input type="date" value={form.expected_close_date} onChange={(e) => setForm((f) => ({ ...f, expected_close_date: e.target.value }))} /></div><div className="space-y-2 sm:col-span-2"><Label>Owner</Label><Select value={form.owner_actor_id} onValueChange={(v) => setForm((f) => ({ ...f, owner_actor_id: v }))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem owner</SelectItem>{actors.map((a) => <SelectItem key={a.id} value={a.id}>{a.display_name}</SelectItem>)}</SelectContent></Select></div></div><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Cancelar</Button><Button disabled={!lead || !form.title.trim() || convert.isPending} onClick={() => lead && convert.mutate({ leadId: lead.id, dealData: { ...form, owner_actor_id: form.owner_actor_id === 'none' ? null : form.owner_actor_id, estimated_value: form.estimated_value ? Number(form.estimated_value) : null } }, { onSuccess: (dealCode) => { toast.success(`Deal ${dealCode} criado a partir de ${lead.code}`); navigate(`/crm/deals/${dealCode}`); } })}>Converter</Button></DialogFooter></DialogContent></Dialog>;
}

export default function CrmLeadDetail() {
  const { code } = useParams<{ code: string }>();
  const { data: lead, isLoading } = useLeadByCode(code);
  const { data: activities = [] } = useActivitiesForEntity('lead', lead?.id);
  const { data: audit = [] } = useEntityAudit('lead', lead?.id);
  const update = useUpdateLeadField(code);
  const { data: actors = [] } = useCrmActors();
  const { data: contacts = [] } = usePeopleByCompany(lead?.company_id ?? null);
  const [title, setTitle] = useState('');
  const [pain, setPain] = useState('');
  const [notes, setNotes] = useState('');
  const [convertOpen, setConvertOpen] = useState(false);
  const [discardOpen, setDiscardOpen] = useState(false);
  const [reason, setReason] = useState('');
  useEffect(() => { if (lead) { setTitle(lead.title); setPain(lead.pain_description ?? ''); setNotes(lead.notes ?? ''); } }, [lead?.id, lead?.title, lead?.pain_description, lead?.notes]);
  const save = (updates: Partial<Lead>) => lead && update.mutate({ id: lead.id, updates });
  if (isLoading) return <DetailShell><Skeleton className="h-8 w-64" /><Skeleton className="mt-4 h-40 w-full" /></DetailShell>;
  if (!lead) return <DetailShell><div className="py-20 text-center"><h1 className="text-2xl font-semibold">Lead não encontrado</h1><Button asChild className="mt-4"><Link to="/crm/leads">Voltar</Link></Button></div></DetailShell>;
  const canConvert = lead.status === 'triagem_feita';
  return <DetailShell><DetailBreadcrumb closeTo="/crm/leads" items={[{ label: 'CRM', to: '/crm/pipeline' }, { label: 'Leads', to: '/crm/leads' }, { label: lead.code }]} /><header className="mb-6 space-y-4 rounded-lg border border-border bg-card/30 p-5"><div className="flex flex-wrap items-center gap-2 text-xs"><span className="font-mono font-semibold text-muted-foreground">{lead.code}</span><InfoBadge><Link to={`/crm/empresas/${lead.company_id}`}>{lead.company?.trade_name || lead.company?.legal_name}</Link></InfoBadge><InfoBadge>{LEAD_LABEL[lead.status]}</InfoBadge></div><Input value={title} onChange={(e) => setTitle(e.target.value)} onBlur={() => title.trim() && title !== lead.title && save({ title: title.trim() })} className="h-auto text-2xl font-semibold" /><div className="flex flex-wrap gap-4 text-sm text-muted-foreground"><span>{lead.source ?? 'sem origem'}</span><span>{lead.owner?.display_name ?? 'sem dono'}</span><span>{formatCurrency(Number(lead.estimated_value ?? 0))}</span></div><div className="flex flex-wrap gap-2"><Button disabled={!canConvert} title={!canConvert ? 'Lead precisa estar em Triagem Feita antes de converter' : undefined} onClick={() => setConvertOpen(true)}>🎯 Converter em Deal</Button>{lead.status !== 'descartado' && lead.status !== 'convertido' && <Button variant="outline" className="text-destructive" onClick={() => setDiscardOpen(true)}>Descartar</Button>}{lead.status === 'descartado' && <Button onClick={() => save({ status: 'novo', lost_reason: null })}>Reabrir lead</Button>}{lead.status === 'convertido' && <InfoBadge>Convertido</InfoBadge>}</div></header><div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_360px]"><main><Tabs defaultValue="details"><TabsList className="h-auto w-full justify-start gap-1 rounded-none border-b border-border bg-transparent p-0"><TabsTrigger value="details">Detalhes</TabsTrigger><TabsTrigger value="activities">Atividades</TabsTrigger><TabsTrigger value="timeline">Timeline</TabsTrigger></TabsList><TabsContent value="details" className="mt-5 space-y-6"><section className="space-y-3"><div className="flex justify-between"><h2 className="text-lg font-semibold">Dor / situação</h2><Button size="sm" variant="outline" onClick={() => save({ pain_description: pain })}>Salvar</Button></div><MarkdownSplitEditor value={pain} onChange={setPain} /></section><section className="space-y-3"><div className="flex justify-between"><h2 className="text-lg font-semibold">Notas</h2><Button size="sm" variant="outline" onClick={() => save({ notes })}>Salvar</Button></div><MarkdownSplitEditor value={notes} onChange={setNotes} minHeight={180} /></section></TabsContent><TabsContent value="activities" className="mt-5"><ActivityPanel entity={{ type: 'lead', id: lead.id }} activities={activities} /></TabsContent><TabsContent value="timeline" className="mt-5"><MiniTimeline rows={audit} /></TabsContent></Tabs></main><aside className="space-y-5 rounded-lg border border-border bg-card/30 p-4"><Field label="Status"><Select value={lead.status} onValueChange={(v) => v !== 'convertido' && save({ status: v as LeadStatus })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{LEAD_STATUS.map((s) => <SelectItem key={s} value={s} disabled={s === 'convertido'}>{LEAD_LABEL[s]}</SelectItem>)}</SelectContent></Select></Field><Field label="Origem"><Input defaultValue={lead.source ?? ''} onBlur={(e) => save({ source: e.target.value || null })} /></Field><Field label="Valor estimado"><Input type="number" defaultValue={lead.estimated_value ?? ''} onBlur={(e) => save({ estimated_value: e.target.value ? Number(e.target.value) : null })} /></Field><Field label="Empresa"><Button asChild variant="outline" className="w-full justify-start"><Link to={`/crm/empresas/${lead.company_id}`}>{lead.company?.trade_name || lead.company?.legal_name}</Link></Button></Field><Field label="Contato"><Select value={lead.contact_person_id ?? 'none'} onValueChange={(v) => save({ contact_person_id: v === 'none' ? null : v })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem contato</SelectItem>{contacts.map((c) => <SelectItem key={c.id} value={c.id}>{c.full_name}</SelectItem>)}</SelectContent></Select></Field><Field label="Owner"><Select value={lead.owner_actor_id ?? 'none'} onValueChange={(v) => save({ owner_actor_id: v === 'none' ? null : v })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Sem owner</SelectItem>{actors.map((a) => <SelectItem key={a.id} value={a.id}>{a.display_name}</SelectItem>)}</SelectContent></Select></Field><Field label="Triagem agendada"><Input type="datetime-local" value={lead.triagem_scheduled_at ? lead.triagem_scheduled_at.slice(0, 16) : ''} onChange={(e) => save({ triagem_scheduled_at: e.target.value ? new Date(e.target.value).toISOString() : null })} /></Field><Field label="Triagem aconteceu"><Input type="datetime-local" value={lead.triagem_happened_at ? lead.triagem_happened_at.slice(0, 16) : ''} onChange={(e) => save({ triagem_happened_at: e.target.value ? new Date(e.target.value).toISOString() : null })} /></Field>{lead.lost_reason && <Field label="Motivo"><p className="text-sm text-muted-foreground">{lead.lost_reason}</p></Field>}</aside></div><ConvertDialog lead={lead} open={convertOpen} onOpenChange={setConvertOpen} /><Dialog open={discardOpen} onOpenChange={setDiscardOpen}><DialogContent><DialogHeader><DialogTitle>Descartar {lead.code}</DialogTitle></DialogHeader><Textarea value={reason} onChange={(e) => setReason(e.target.value)} placeholder="Motivo obrigatório" /><DialogFooter><Button variant="outline" onClick={() => setDiscardOpen(false)}>Cancelar</Button><Button variant="destructive" disabled={!reason.trim()} onClick={() => save({ status: 'descartado', lost_reason: reason })}>Descartar</Button></DialogFooter></DialogContent></Dialog></DetailShell>;
}
