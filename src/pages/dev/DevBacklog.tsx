import { PlaceholderTab } from "@/components/dev/PlaceholderTab";

export default function DevBacklog() {
  return (
    <PlaceholderTab
      title="Backlog"
      promptCode="próximo prompt"
      description="Lista priorizável de tarefas sem sprint atribuída. Ponte entre 'ideia/bug reportado' e o Kanban da sprint."
    />
  );
}
